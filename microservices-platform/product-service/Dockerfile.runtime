# Imagen solo runtime: requiere target/*.jar ya generado (p. ej. tras mvn clean install en CI).
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "app.jar"]
